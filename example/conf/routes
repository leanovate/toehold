# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Home page
GET         /                    controllers.Application.index

# Map static resources from the /public folder to the /assets URL path
GET         /assets/*file        controllers.Assets.at(path="/public", file)

POST        /*path.php           de.leanovate.play.fastcgi.FastCGIController.serve(documentRoot = "./app-php", path, extension = ".php")
GET         /*path.php           de.leanovate.play.fastcgi.FastCGIController.serve(documentRoot = "./app-php", path, extension = ".php")

GET         /*path               de.leanovate.play.fastcgi.FileAssetsController.file(documentRoot = "./example/app-php", path)