# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Home page
GET         /                    controllers.Application.index

# Map static resources from the /public folder to the /assets URL path
GET         /assets/*file        controllers.Assets.at(path="/public", file)

GET         /$path<.+>$extension<\.php[345]?>   de.leanovate.play.fastcgi.FastCGIController.serve(path, extension)
POST        /$path<.+>$extension<\.php[345]?>   de.leanovate.play.fastcgi.FastCGIController.serve(path, extension)

GET         /*path               de.leanovate.play.fastcgi.FileAssetsController.file(path)